<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Global Bank Pro V2</title>
    <style>
        .id-badge { font-family: monospace; font-size: 0.9rem; }
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Global Bank Pro</h1>
            <div>
		<!-- Format: bank_stack_frontend.1 -->
                <span class="badge bg-primary p-2">Frontend: <%= frontendId %></span>
                <!-- Format: bank_stack_backend-api.7 -->
                <span class="badge bg-danger p-2">Backend: <%= backendId %></span>
                <!-- Shows which Frontend container HAProxy picked -->
                <!--span class="badge bg-primary id-badge">Frontend ID: <%= frontendId %></span-->
                <!-- Shows which Backend container the Frontend picked -->
                <!--span class="badge bg-danger id-badge">Backend ID: <%= backendId %></span-->
            </div>
        </div>

        <div class="row">
            <!-- ... (Rest of the code: Account Card and Quick Deposit Form same as before) ... -->
            <div class="col-md-4">
                <div class="card shadow-sm mb-4">
                    <div class="card-body">
                        <h5>Account: <%= account.accountNumber %></h5>
                        <h2 class="text-success"><%= account.balance %> <%= account.currency %></h2>
                        <p class="text-muted">Owner: <%= account.ownerName %></p>
                    </div>
                </div>
                
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h6>Quick Deposit</h6>
                        <form action="/deposit" method="POST">
                            <input type="number" name="amount" class="form-control mb-2" placeholder="Amount (DZD)" required>
                            <button class="btn btn-primary w-100">Deposit Funds</button>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h6>Recent Transactions</h6>
                        <table class="table">
                            <thead><tr><th>Date</th><th>Desc</th><th>Amount</th></tr></thead>
                            <tbody>
                                <% account.Transactions.reverse().forEach(t => { %>
                                    <tr>
                                        <td><%= new Date(t.date).toLocaleDateString() %></td>
                                        <td><%= t.description %></td>
                                        <td class="text-success">+ <%= t.amount %></td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
